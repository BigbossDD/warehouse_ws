<!-- this code will show gazebo and rviz -->

<launch>

    <!-- ========================================================= -->
    <!-- Robot Description                                         -->
    <!-- ========================================================= -->
    <let name="robot_description_command"
         value="$(command 'xacro $(find-pkg-share warehouse_robot)/urdf/warehouse_robot.urdf.xacro')" />

    <!-- ========================================================= -->
    <!-- Robot State Publisher                                     -->
    <!-- NOTE: use_sim_time is required to prevent TF warnings    -->
    <!-- like TF_OLD_DATA (RViz rejecting old timestamps from     -->
    <!-- Gazebo simulation clock mismatch).                        -->
    <!-- ========================================================= -->
    <node
        pkg="robot_state_publisher"
        exec="robot_state_publisher"
        name="robot_state_publisher"
        output="screen">

        <param name="robot_description"
               value="$(var robot_description_command)" />

        <!-- FIX for TF_OLD_DATA warnings -->
        <param name="use_sim_time" value="true"/>

    </node>

    <!-- ========================================================= -->
    <!-- Joint State Publisher                                     -->
    <!-- NOTE: GUI is optional; can also cause early TF timestamps -->
    <!-- ========================================================= -->
    <node
        pkg="joint_state_publisher_gui"
        exec="joint_state_publisher_gui"
        name="joint_state_publisher_gui"
        output="screen"
    />

    <!-- ========================================================= -->
    <!-- Gazebo                                                   -->
    <!-- ========================================================= -->
    <include file="$(find-pkg-share gazebo_ros)/launch/gazebo.launch.py">
        <arg name="verbose" value="true"/>
    </include>

    <!-- ========================================================= -->
    <!-- Spawn Robot                                               -->
    <!-- ========================================================= -->
    <node
        pkg="gazebo_ros"
        exec="spawn_entity.py"
        name="spawn_entity"
        output="screen"
        args="-topic robot_description -entity warehouse_robot" />

    <!-- ========================================================= -->
    <!-- RViz                                                      -->
    <!-- NOTE: use_sim_time avoids TF timestamp mismatch warnings -->
    <!-- ========================================================= -->
    <node
        pkg="rviz2"
        exec="rviz2"
        name="rviz2"
        output="screen">

        <!-- FIX for TF_OLD_DATA warnings -->
        <param name="use_sim_time" value="true"/>

    </node>

</launch>